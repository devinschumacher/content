# view sheets in excel files - python, pandas

You can view a list of sheet names from an excel spreadsheet using the `.sheet_names` method.

**Example:**

```python
import pandas as pd

# save excel file to variable
file = 'spreadsheet.xlsx'

# use pandas pd.ExcelFile() function to save the file to a variable 'ss' (for spreadsheet)
ss = pd.ExcelFile(file)

# print the sheet names in the spreadsheet 'ss' using the pandas .sheet_names method
print(ss.sheet_names)

# output
# ['2002', '2004']
```

Continuing, let’s load a DataFrame and view it:

```python

# load sheet '2004' into a DataFrame (by it's name) using the .parse()
df1 = ss.parse('2004')

# load sheet '2002' into a DataFrame (by it's index) using the .parse()
df2 = ss.parse(0)

# print the head of the dataframes
print(df1.head())
print(df2.head())
```