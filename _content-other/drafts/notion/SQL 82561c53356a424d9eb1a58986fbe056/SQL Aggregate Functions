# SQL Aggregate Functions

---

- `COUNT` counts how many rows are in a column
- `SUM` adds values

Aggregate functions can perform operations on columns, unlike arithmetic operators which perform functions on rows.

However: A**ggregators can *only* aggregate vertically.**